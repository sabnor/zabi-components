export interface CardData {
    title: string;
    description: string;
    image: string;
}

export interface FormData {
    name: string;
    email: string;
}

export interface NavItem {
    label: string;
    href: string;
}
